apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kcm.fullname" . }}-test-connection"
  labels:
    {{- include "kcm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kcm.fullname" . }}:{{ .Values.service.port }}/health']
  restartPolicy: Never
